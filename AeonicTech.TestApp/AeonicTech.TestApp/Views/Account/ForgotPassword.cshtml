@model AeonicTech.TestApp.ViewModels.ForgotPasswordViewModel
@{
    ViewData["Title"] = "Forgot Password";
    Layout = "~/Views/Shared/_LayoutLogin.cshtml";
}

<div class="row">
    <div class="col-12 mt-6">
        <form method="post" asp-area="" asp-controller="Account" asp-action="ForgotPassword" class="form-signin text-center">
            <h1 class="mb-3 font-weight-normal">Forgot password</h1>
            <div class="validation-summary mb-3">
                <div asp-validation-summary="All" class="text-danger"></div>
            </div>
            <div class="form-group mb-3">
                <label for="Email" class="sr-only">Email address</label>
                <input type="email" id="Email" name="Email" class="form-control" placeholder="Email address" required autofocus>
            </div>
            <div class="form-group mb-3">
                <div class="row">
                    <div class="col-6 text-left">
                        <a href="@Url.Action("Login", "Account", new { Area = "" })" class="txt3">
                            Back to log in
                        </a>
                    </div>
                    <div class="col-6 text-right">
                        <a href="@Url.Action("Register", "Account", new { Area = "" })" class="txt3">
                            Create a new account
                        </a>
                    </div>
                </div>
            </div>
            <div class="mt-5">
                <button class="btn btn-lg btn-primary btn-block" type="submit">Reset password</button>
            </div>
        </form>
    </div>
</div>